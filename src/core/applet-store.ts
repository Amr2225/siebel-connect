// applet-store.ts — createAppletStore(applet) -> { subscribe, getSnapshot, destroy }.
// Implemented in Phase 10 — React Adapter (feature/phase-10-react-adapter).
// Placeholder — structure only.
export {}
