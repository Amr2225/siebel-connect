// factory.ts — init / getApplet / getPopup / clear (memoized, AppletRegistry-typed; was NexusFactory).
// Implemented in Phase 09 — Typed Factory & Registry (feature/phase-9-factory).
// Placeholder — structure only.
export {}
